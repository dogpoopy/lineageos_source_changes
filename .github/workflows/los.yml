name: LineageOS Source Changes Tracker

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  lineage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git-core python3-pip
          pip install GitPython lxml
          wget https://storage.googleapis.com/git-repo-downloads/repo
          sudo mv repo /usr/bin/repo
          sudo chmod a+x /usr/bin/repo

      - name: Initialize LineageOS manifest
        run: |
          mkdir workspace && cd workspace
          repo init --no-repo-verify -u https://github.com/LineageOS/android.git -b lineage-22.2 -g default,-mips,-darwin,-notdefault
          repo manifest -o source_manifest.xml

      - name: Generate source changes report
        run: |
          python3 scripts/parse_source.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "🔄 Fetch latest LineageOS source changes"
          git push
